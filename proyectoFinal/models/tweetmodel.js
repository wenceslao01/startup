Tweet=Backbone.Model.extend({
});


Tweets=Backbone.Collection.extend({
	url: "http://localhost:3000/timeline?count=50"

});
BlackUser=Backbone.Model.extend({
});
BlackList=Backbone.Collection.extend({
});
